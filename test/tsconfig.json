{
    // Used to type-check the tests in the editor and by eslint, nothing is emitted from here
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "rootDir": "..",
        "noEmit": true,
        "declaration": false
    },
    "include": ["**/*.ts"]
}
